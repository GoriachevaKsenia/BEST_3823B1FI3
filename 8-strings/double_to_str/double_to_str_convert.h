#include <math.h>
#include <string.h>
#include<stdlib.h>

char* convert(double number){
    /*double number_copy = number;
    int num_copy = number;
    int count = strlen(number);
   
    char* res = malloc(count);
    int i = 0;
    count --;
    while(count != -1){
        res[i] = '0' + number_copy / (int)pow(10, count);
        number_copy %= (int)pow(10, count);
        count --;
        i++;
    }
    return res;
}
 while (number != 0){
        count ++;
        number/= 10;
    }*/
    return NULL;
}  