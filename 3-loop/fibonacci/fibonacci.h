long fib(int m){
   /*int f;
   while (f <= m)
    {
         if (f == 0)
         return 0;
         if (f == 1)
         return 1; 
         if (f > 1) 
         return fib (f-2) + fib (f-1);
    }*/
    return 0;
}